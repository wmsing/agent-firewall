---
description: Jev Python 示例约定
globs: jev/**/*
alwaysApply: false
---

# Jev demo (Python)

- 需求对齐：[`docs/spec/task_jev_triage.md`](../../docs/spec/task_jev_triage.md)
- Python **3.14+**；依赖见 `jev/requirements.txt`（`jev==0.3.0`）。
- 默认离线 `python example.py`；有 `TYPESAFE_API_KEY` 时用 `--live`。
- 类型用 Pydantic；`@jev.fn` + docstring 模板渲染 state。
