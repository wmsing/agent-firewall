---
description: 仓库协作地图与通用原则
alwaysApply: true
---

# Repo

- 独立仓库根即 Go module：`main.go`（L7）· `cmd/mcp-firewall` · `eval/` · `docs/spec/`。
- 新功能：先 `docs/spec/task_*.md` → 对话 `@` 该 spec → 实现 → `make check-firewall`。
- 勿提交 `.env`；勿擅自改生产密钥。
